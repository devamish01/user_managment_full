/**
 * useUsersStore — public hook alias.
 * The implementation lives in `../store/users.store`.
 * Consumers should import from `@/modules/users` or `@/modules/users/hooks`.
 */
export { useUsersStore } from "../store/users.store";
export type { UsersStoreState } from "../store/users.store";
export { default } from "../store/users.store";
