/**
 * Routes barrel — re-exports that are still consumed by live code.
 * RouteView, routeGuards, routeTypes and routeConfig have been removed.
 */
export type { RouteName } from "@/shared/constants/routes";
