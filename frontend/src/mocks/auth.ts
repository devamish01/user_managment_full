export const mockAuth = {
  accessToken: "",
  refreshToken: "",
  currentUserId: "", // No session by default — must come from token
  rememberMe: true,
  loginAt: "",
  expiresAt: "",
};
export const superAdminPassword = {
  current: "SuperSecret123!"
};
