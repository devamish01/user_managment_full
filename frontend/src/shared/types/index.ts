/**
 * Shared Types Index
 * Generic types used across multiple modules.
 * Domain-specific types (User, Role, Permission) stay in src/lib/types.ts
 * until module migration. API envelope types stay in src/api/types.ts.
 */

export * from "./common";
export * from "./pagination";
export * from "./table";
