# Shared Components

Reusable, domain-agnostic UI primitives.

Migration target for `src/components/ui/` (Button, Card, Dialog,
Drawer, Select, Switch, Badge, Avatar, Tabs, Skeleton, Toast, ...).

Rules:
- No business logic.
- No store imports.
- No module imports.
- Props-driven only.
